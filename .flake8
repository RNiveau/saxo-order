[flake8]
ignore = E203, W503, W504
# node_modules holds vendored Python that is not ours to lint; without this,
# running the documented gate after an `npm install` buries real findings.
exclude = .git,__pycache__,frontend/node_modules,.venv
