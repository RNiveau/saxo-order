[tool.poetry]
name = "saxo-order"
version = "0.1.0"
description = ""
authors = ["Kiva"]

[tool.poetry.dependencies]
python = "^3.9"
click = "^8.0.1"
requests = "^2.26.0"
pyyaml = "^6.0"

[tool.poetry.dev-dependencies]

[tool.poetry.scripts]
auth = "saxo_order.cli:auth"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"